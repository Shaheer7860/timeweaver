# ⚠️ IMPORTANT: Netlify does NOT support Flask applications directly!
# 
# This file is for reference only. To deploy this Flask app, use:
# - Railway (recommended): https://railway.app
# - Render: https://render.com  
# - Vercel: https://vercel.com
#
# See NETLIFY_SETUP.md for details

[build]
  command = "echo 'Flask apps not supported on Netlify. Use Railway or Render instead. See NETLIFY_SETUP.md'"
  publish = "public"

# Static file serving only (won't run Flask backend)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  PYTHON_VERSION = "3.11"

